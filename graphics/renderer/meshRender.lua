local MeshRender = class 'MeshRender'

function MeshRender.setup()
end

function MeshRender:init()
end

function MeshRender:release()
end

function MeshRender:render(shader, drawMode, img, x, y, z, w, h, d, nInstances)
end
