uniform vec4 fill;
uniform vec4 stroke;

uniform int useColor;
uniform int useTexture;

in vec4 vColor;

void main() {
    if (useColor == 1)
        gl_FragColor = vColor;
    else
        gl_FragColor = fill;
}
