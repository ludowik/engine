attribute vec3 vertex;

uniform vec4 matrixProjection;

void main() {
    gl_Position = vec4(
        (vertex.x + matrixProjection.x) / matrixProjection.z,
        (vertex.y + matrixProjection.y) / matrixProjection.w,
        1.,
        1.);
}
