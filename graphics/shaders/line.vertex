attribute vec3 vertex;

uniform vec4 stroke;

uniform mat4 matrixModel;
uniform mat4 matrixPV;

uniform vec3 pos;
uniform vec3 size;

void main() {
    vec3 vertexPosition = vertex * size + pos;
    gl_Position = matrixModel * vec4(vertexPosition.xyz, 1.);
}
