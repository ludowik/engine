attribute vec2 point;

uniform mat4 matrixModel;
uniform mat4 matrixPV;

uniform vec3 pos;
uniform vec3 size;

void main() {
    vec3 vertexPosition = vec3(point.xy, 0.) * size + pos;
    gl_Position = matrixPV * matrixModel * vec4(vertexPosition.xyz, 1.);
}
